{
  "name": "patient-lab-test-portal",
  "version": "1.0.0",
  "description": "A full-stack patient portal for digital health clinic allowing patients to register, view lab tests, book appointments, and download reports",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": [
    "patient-portal",
    "healthcare",
    "lab-tests",
    "booking-system",
    "react",
    "express",
    "mongodb",
    "jwt"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
